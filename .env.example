# Optional: dynamic saves without redeploy (Upstash Redis / Vercel KV)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Fallback / single-site config (works on Vercel without Redis)
WEBSITE_DOMAIN=tademehl.com
AGENT_WEBHOOK_URL=
AGENT_SECRET=

# Protect the setup UI (recommended in production)
SETUP_PASSWORD=
